import templateUrl from './livecameraview.template.html';
import Controller from './livecameraview.controller.js';

let Component = {
  restrict: 'E',
  templateUrl,
  controller: Controller
};

export default Component;
