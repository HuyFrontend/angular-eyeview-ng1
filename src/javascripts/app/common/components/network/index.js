import Component from './network.component';

let AppMainNetworkComponent = angular.module('app.main.components.network', [])
  .component('network', Component);

export default AppMainNetworkComponent;
